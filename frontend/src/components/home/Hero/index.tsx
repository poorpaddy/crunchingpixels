import React from "react";
import Header from "@layout/Header";

const Hero: React.FC = () => {
  return (
    <main className="bg-home bg-cover">
      <Header />
    </main>
  );
};

export default Hero;
